# API Configuration
# NOTE: Default values now come from ../shared_config.yaml
# Environment variables below can override shared config values

# Model Settings (overrides shared config if set)
# MODEL_PATH=models/checkpoint-best
# DEVICE=auto

# Server Settings (overrides shared config if set)
# HOST=0.0.0.0
# PORT=8000
# RELOAD=True

# Data Processing (overrides shared config if set)
# MAX_SENTENCES=120
# MAX_WORDS_PER_SENTENCE=50
# BATCH_SIZE=8
