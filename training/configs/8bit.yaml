# 8-bit quantization training configuration
# WARNING: Has known compatibility issues with DeBERTa v2 + LoRA
# Consider using stable.yaml instead

# Enable 8-bit quantization (override from shared config)
load_in_8bit: true
