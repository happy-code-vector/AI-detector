# 4-bit QLoRA training configuration
# WARNING: Has known compatibility issues with DeBERTa v2 + LoRA
# Consider using stable.yaml instead

# Enable 4-bit quantization (override from shared config)
load_in_4bit: true
