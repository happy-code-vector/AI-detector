# Stable training configuration
# Uses shared_config.yaml as base - this file only contains overrides
# To see all defaults, check ../shared_config.yaml

# Model override (optional - uses shared config default if not specified)
model: microsoft/deberta-v3-base

# Training-specific overrides (all inherit from shared config if omitted)
# See shared_config.yaml for full list of available parameters
